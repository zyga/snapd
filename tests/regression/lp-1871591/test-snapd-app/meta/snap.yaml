name: test-snapd-app
version: 1
architectures: [all]
base: core18
confinement: strict
layout:
  /etc/mke2fs.conf:
    bind-file: $SNAP_COMMON/etc/mke2fs.conf
  /etc/mtools.conf:
    bind-file: $SNAP_COMMON/etc/mtools.conf
apps:
    sh:
        command: bin/sh
        plugs:
            - home
            - mount-observe
            - network-observe
            - ssh-public-keys
